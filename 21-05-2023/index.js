//We need a function that can transform a number (integer) into a string.

const numToStr=(num)=> {
    return num.toString()  // tostring is inbuilt function that convert num to string
}

console.log(numToStr(123)); 

//