//Retrieve the value of an input element with the id "myInput" and store it in a variable called "inputValue".

let btn=document.querySelector('.btn');
btn.addEventListener('click',(e)=>{
    e.preventDefault
       let value=document.getElementsByClassName('inputs').value
        console.log(value);
   })
   
